{{!-- <style>
  body {}

  .trip-summary-container {
    background: rgb(99, 194, 231);
    color: aliceblue;
    padding: 20px;
    height: 100%;
  }

  .profile-pic {
    float: left;
  }

  img {
    border-radius: 50%;
    margin: auto;
    margin-right: 20px;
  }

  .trip-interests {
    list-style: none;
    display: inline;
  }
</style>
<br>
<br>
<br>
<div class="container">
  <div class="trip-summary-container s12 l10">
    <div class="profile-pic">
      <img src="https://www.fillmurray.com/200/200" style="float: left;" />
    </div>
    <h5>{{trip.startDate}}, {{trip.endDate}}</h5>
    <h2 class="city-name display-4">{{trip.destination}}</h1>
      <p class="trip-summary lead">{{trip.remark}}</p>
      <ul>
        {{#each tripInterests}}
        <li class="interests" id="{{this.text}}"> <span class="new badge blue">{{this.text}}</span></li>
        {{/each}}
      </ul>
  </div>
</div>
</div>
</div> --}}

{{!-- kimmy's testing --}}


{{> nav/navbar}}



<br><br>
<div class="container">
  <div class="trip-summary-container s12 l10">
    <div class="profile-pic">
      <img src="https://picsum.photos/640/360" style="float: left;" />
    </div>
    <div class="profile-data">
      <h2 class="display-4 profile-name">{{trip.destination}}</h2>
      <h5>{{trip.start_date}}</h5>
      <p class="lead">{{trip.start_date}}</p>
      <h5>{{trip.trip_interests}}</h5>
    </div>
    <button class="waves-effect waves-light btn-small edit">edit</button>
    <button class="waves-effect waves-light btn-small red lighten-1 delete">remove</button>
  </div>
</div>